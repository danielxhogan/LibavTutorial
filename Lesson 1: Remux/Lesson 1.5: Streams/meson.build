project('Lesson 1.5 - Streams', 'c')

lavf = dependency('libavformat')
lavc = dependency('libavcodec')
lavu = dependency('libavutil')

executable('streams', ['streams.c'], dependencies: [lavf, lavc, lavu])
